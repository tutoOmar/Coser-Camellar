import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coser.y.camellar',
  appName: 'Coser & Camellar',
  webDir: 'dist/tu-chamba/browser',
};

export default config;
